{
  "name": "problem1",
  "version": "1.0.0",
  "description": "Develop a HTTP microservice called number-management-service that exposes a GET REST API \"/numbers\". This API receives a list of URLs through query parameters. This query param is called \"url\". It can appear more than once, for example:",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.26.0",
    "express": "^4.17.3"
  }
}
